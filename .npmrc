# @astrojs/svelte 8.x declares a typescript@^5 peer, but this project is on TS 6
# (a peer-range lag, not a real incompatibility). Accept it so `npm ci` in CI
# reproduces the same tree the lockfile was generated with.
legacy-peer-deps=true
