/* exported API */
var API = {
  catalog: '/catalog',
  settings: '/settings',
  sections: '/sections',
  section: function(id) { return '/sections/' + id; },
  sectionEntries: function(id) { return '/sections/' + id + '/entries'; },
  sectionEntriesOrder: function(id) { return '/sections/' + id + '/entries/order'; },
  documents: function(variant) { return '/documents/' + variant; },
  entry: function(id) { return '/entries/' + id; },
  entryItems: function(id) { return '/entries/' + id + '/items'; },
  entryItemsOrder: function(id) { return '/entries/' + id + '/items/order'; },
  item: function(id) { return '/items/' + id; },
  coverletterSections: '/coverletter/sections',
  coverletterSection: function(id) { return '/coverletter/sections/' + id; },
  persons: '/persons',
  person: function(id) { return '/persons/' + id; },
  personSwitch: function(id) { return '/persons/' + id + '/switch'; },
  personSave: function(id) { return '/persons/' + id + '/save'; },
  compile: function(name) { return '/compile/' + name; },
  pdf: function(name) { return '/pdf/' + name; },
  export: '/export',
  import: '/import',
};
