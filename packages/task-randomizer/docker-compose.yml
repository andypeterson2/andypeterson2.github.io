services:
  task-randomizer:
    image: node:20-slim
    command: node serve.mjs
    ports:
      - "127.0.0.1:${PORT:-8070}:${PORT:-8070}"
    environment:
      PORT: ${PORT:-8070}
    volumes:
      - .:/app
    working_dir: /app
