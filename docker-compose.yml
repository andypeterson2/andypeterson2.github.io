services:
  tech-tree:
    image: node:20-slim
    command: node serve.mjs
    ports:
      - "127.0.0.1:${PORT:-8080}:${PORT:-8080}"
    environment:
      PORT: ${PORT:-8080}
    volumes:
      - .:/app
    working_dir: /app
