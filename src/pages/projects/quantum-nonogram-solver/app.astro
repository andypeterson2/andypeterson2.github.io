---
import DemoShell from '../../../layouts/DemoShell.astro';
---

<DemoShell
  title="Nonogram Solver"
  backend={{ service: 'nonogram', port: 5055, label: 'Nonogram Backend' }}
>
  <!-- App shell -->
  <div id="nonogram-app">
    <div class="nono-columns">
      <div class="nono-left">
        <div class="section-rule"><span>Puzzle Editor</span></div>
        <div id="draw-view"></div>
        <div class="editor-actions">
          <button id="btn-clear" class="nono-btn">Clear</button>
          <button id="btn-random" class="nono-btn">Randomize</button>
        </div>
        <div class="grid-size-controls">
          <button id="btn-add-row" class="nono-btn nono-btn-sm">+ Row</button>
          <button id="btn-add-col" class="nono-btn nono-btn-sm">+ Col</button>
          <span id="grid-size-label">3 &times; 3</span>
        </div>
        <div id="status-line" class="status-line">Ready</div>
      </div>
      <div class="nono-right">
        <div class="controls-bar">
          <label class="trials-label"
            >Trials
            <input type="number" id="trials-input" min="1" max="20" value="1" />
          </label>
          <button id="btn-bench" class="nono-btn nono-btn-run">&#9654; Run on Simulator</button>
          <label class="gallery-label"
            >Gallery
            <select id="gallery-select">
              <option value="">— real quantum runs —</option>
            </select>
          </label>
        </div>
        <div class="section-rule"><span>Classical</span></div>
        <div id="cl-canvas" class="sol-canvas">
          <div id="cl-placeholder" class="placeholder-msg">Run Benchmark to see solutions.</div>
        </div>
        <div class="section-rule"><span>Quantum</span></div>
        <div id="qu-list" class="sol-canvas">
          <div id="qu-sol-placeholder" class="placeholder-msg">Run Benchmark to see solutions.</div>
        </div>
        <div class="section-rule"><span>Histogram</span></div>
        <div class="histogram-controls">
          <label class="threshold-label"
            >Thr
            <input type="number" id="threshold-input" min="0" max="100" step="0.01" value="0.50" />
            <span>%</span>
          </label>
        </div>
        <div id="qu-area" class="histogram-area">
          <svg id="qu-histogram" width="100%" height="100%" aria-hidden="true"></svg>
          <div id="qu-placeholder" class="placeholder-msg"></div>
        </div>
        <div class="section-rule"><span>Metrics</span></div>
        <div id="metrics-pane"></div>
      </div>
    </div>
  </div>

  <!-- Vendored same-origin (SRI-verified against the previously pinned CDN
       hash at vendoring time) — closes the last third-party origin in the
       CSP. Version must match the nonogram backend's socket.io family. Stays a
       classic script; it supplies the `io` global the bundled app consumes. -->
  <script is:inline src="/vendor/socket.io-4.7.5.min.js"></script>
  <!-- Nonogram tier (bundled module): state/grid/solver/classical-solver/app. -->
  <script>
    import '../../../apps/nonogram/entry';
  </script>
</DemoShell>

<style>
  /* ── Buttons ── */
  .nono-btn {
    background: var(--paper);
    border: 2px solid var(--ink);
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    padding: 6px 14px;
    cursor: pointer;
    user-select: none;
  }

  .nono-btn:hover {
    background: var(--ink);
    color: var(--paper);
  }

  .nono-btn:disabled {
    opacity: 0.4;
    cursor: default;
  }

  .nono-btn:disabled:hover {
    background: var(--paper);
    color: var(--ink);
  }

  .nono-btn-sm {
    font-size: var(--text-3xs);
    padding: 4px 10px;
  }

  .nono-btn-run {
    background: var(--ink);
    color: var(--paper);
    font-size: var(--text-3xs);
    padding: 6px 16px;
  }

  .nono-btn-run:hover {
    background: var(--ink-2);
  }

  .nono-btn-run:disabled {
    opacity: 0.4;
  }

  /* ── Two-column layout ── */
  .nono-columns {
    display: flex;
    gap: 0;
    min-height: 600px;
  }

  .nono-left {
    width: 40%;
    border-right: 2px solid var(--ink);
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .nono-right {
    width: 60%;
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  /* ── Draw view / grid ── */
  :global(#draw-view) {
    display: flex;
    justify-content: center;
    user-select: none;
  }

  :global(.nonogram-table) {
    border-collapse: collapse;
  }

  :global(.nonogram-table td.cell) {
    width: 24px;
    height: 24px;
    border: 1px solid var(--ink);
    background: var(--paper);
    cursor: crosshair;
  }

  :global(.nonogram-table td.cell.filled) {
    background: var(--ink);
  }

  :global(.nonogram-table .corner-cell) {
    background: transparent;
  }

  :global(.nonogram-table .row-clue),
  :global(.nonogram-table .col-clue) {
    background: var(--dither-light);
    text-align: center;
    font-size: var(--text-3xs);
    font-weight: 600;
    border: 1px solid var(--paper-4);
    padding: 2px;
  }

  :global(.row-clue-slots) {
    display: flex;
    gap: 2px;
    justify-content: flex-end;
    padding: 0 4px;
  }

  :global(.col-clue-slots) {
    display: flex;
    flex-direction: column;
    gap: 1px;
    align-items: center;
    padding: 2px 4px;
  }

  :global(.clue-slot) {
    font-size: var(--text-4xs);
    font-weight: 700;
    color: var(--ink-2);
    min-width: 12px;
    text-align: center;
  }

  :global(.clue-slot.empty) {
    visibility: hidden;
  }

  /* ── Editor actions ── */
  .editor-actions {
    display: flex;
    gap: 8px;
    justify-content: center;
  }

  .grid-size-controls {
    display: flex;
    gap: 8px;
    align-items: center;
    justify-content: center;
  }

  :global(#grid-size-label) {
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    color: var(--ink-2);
  }

  /* ── Status line ── */
  .status-line {
    font-size: var(--text-3xs);
    color: var(--color-text-muted);
    padding: 4px 0;
    text-align: center;
  }

  :global(.status-line.status-err) {
    color: var(--color-danger);
  }

  :global(.status-line.status-ok) {
    color: var(--color-success);
  }

  /* ── Controls bar ── */
  .controls-bar {
    display: flex;
    gap: 12px;
    align-items: center;
  }

  .trials-label {
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .trials-label input {
    width: 50px;
    border: 2px solid var(--ink);
    background: var(--paper);
    padding: 4px 6px;
    font-size: var(--text-3xs);
    text-align: center;
  }

  /* Gallery — pre-computed REAL quantum runs, rendered with no backend. */
  .gallery-label {
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    display: flex;
    align-items: center;
    gap: 6px;
    margin-left: auto;
  }

  .gallery-label select {
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    border: 2px solid var(--ink);
    background: var(--paper);
    padding: 3px 6px;
    max-width: 190px;
  }

  /* ── Solution canvases ── */
  .sol-canvas {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    min-height: 40px;
  }

  :global(.placeholder-msg) {
    color: var(--ink-4);
    font-size: var(--text-3xs);
    font-style: italic;
    padding: 8px 0;
  }

  /* #qu-placeholder starts hidden and the solver reveals it with an explicit
     display:block. A CSS rule, not a style="display:none" attribute — the strict
     hashed CSP blocks inline style attributes. */
  :global(#qu-placeholder) {
    display: none;
  }

  :global(.sol-grid-wrap) {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
  }

  :global(.sol-grid-label) {
    font-size: var(--text-4xs);
    color: var(--color-text-muted);
    font-weight: 600;
  }

  :global(.sol-table) {
    border-collapse: collapse;
  }

  :global(.sol-table.sz-sm td) {
    width: 10px;
    height: 10px;
  }

  :global(.sol-table.sz-md td) {
    width: 16px;
    height: 16px;
  }

  :global(.sol-table.sz-lg td) {
    width: 22px;
    height: 22px;
  }

  :global(.sol-table td) {
    border: 1px solid var(--paper-4);
  }

  :global(.sol-table td.f) {
    background: var(--ink);
  }

  :global(.sol-table td.e) {
    background: var(--paper);
  }

  /* ── Histogram ── */
  .histogram-controls {
    display: flex;
    gap: 8px;
  }

  .threshold-label {
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .threshold-label input {
    width: 60px;
    border: 2px solid var(--ink);
    background: var(--paper);
    padding: 4px 6px;
    font-size: var(--text-3xs);
    text-align: center;
  }

  .histogram-area {
    height: 220px;
    border: 2px solid var(--ink);
    position: relative;
    background: var(--paper);
    overflow: hidden;
  }

  /* ── Metrics ── */
  :global(#metrics-pane) {
    font-size: var(--text-3xs);
  }

  :global(#metrics-pane:not(.visible)) {
    display: none;
  }

  :global(.metrics-table) {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--text-3xs);
  }

  :global(.metrics-table th) {
    background: var(--ink);
    color: var(--paper);
    font-family: var(--font-ui);
    font-size: var(--text-4xs);
    text-transform: uppercase;
    padding: 4px 8px;
    text-align: left;
  }

  :global(.metrics-table td) {
    border: 1px solid var(--paper-4);
    padding: 3px 8px;
  }

  :global(.metrics-table .metric-label) {
    font-weight: 600;
    background: var(--paper-2);
  }
</style>
