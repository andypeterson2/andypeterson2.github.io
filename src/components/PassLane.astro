<!-- Every page: reads and scrubs a ?pass= / #pass= token and installs the fetch wrapper
     that carries it. Demo pages add the rest of the live tier (LiveTier, via DemoShell). -->
<script>
  import '../apps/shared/pass';
</script>
