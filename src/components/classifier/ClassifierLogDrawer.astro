<div id="log-drawer" class="log-drawer">
  <!-- A real button the keyboard can reach, labelled and bordered so it reads as the
       handle it is. -->
  <button
    id="log-handle"
    class="log-handle"
    type="button"
    aria-expanded="false"
    aria-controls="log-terminal"
  >
    <span class="log-caret" aria-hidden="true"></span>Log
  </button>
  <pre id="log-terminal" class="log-terminal"></pre>
</div>

<style>
  .log-drawer {
    border-top: 2px solid var(--ink);
    background: var(--paper);
    flex-shrink: 0;
  }

  .log-handle {
    all: unset;
    box-sizing: border-box;
    display: flex;
    align-items: center;
    gap: 6px;
    width: 100%;
    min-height: 28px;
    padding: 2px 8px;
    font-family: var(--font-ui);
    font-size: var(--text-3xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: var(--paper);
    color: var(--ink);
    cursor: pointer;
    border-bottom: 1px solid var(--ink);
  }

  .log-handle:hover {
    background: var(--ink);
    color: var(--paper);
  }

  .log-handle:focus-visible {
    outline: 2px solid var(--ink);
    outline-offset: -4px;
  }

  /* A System-6 disclosure triangle: points up to open the log taller, down to shrink. */
  .log-caret {
    width: 0;
    height: 0;
    border-inline: 4px solid transparent;
    border-bottom: 5px solid currentcolor;
  }

  .log-handle[aria-expanded='true'] .log-caret {
    border-bottom: 0;
    border-top: 5px solid currentcolor;
  }

  @media (pointer: coarse) {
    .log-handle {
      min-height: 44px;
    }
  }

  /* Closed: the newest line. Open: twelve lines, scrolling. */
  .log-terminal {
    font-family: var(--font-mono);
    font-size: var(--text-3xs);
    line-height: 1.4;
    padding: 4px 8px;
    overflow: hidden;
    max-height: calc(1.4em + 8px);
    margin: 0;
    white-space: pre-wrap;
    word-break: break-all;
  }

  .log-drawer.open .log-terminal {
    overflow-y: auto;
    max-height: calc(12 * 1.4em + 8px);
  }

  :global(.log-entry) {
    display: flex;
    gap: 8px;
    line-height: 1.4;
  }

  :global(.log-time) {
    color: var(--ink-3);
    flex-shrink: 0;
  }

  :global(.log-msg) {
    color: var(--ink-2);
  }

  :global(.log-ok) {
    color: var(--color-success);
  }

  :global(.log-err) {
    color: var(--color-danger);
  }
</style>
