services:
  website:
    image: python:3.12-slim
    command: python -m http.server ${PORT:-8000}
    ports:
      - "127.0.0.1:${PORT:-8000}:${PORT:-8000}"
    volumes:
      - .:/app
    working_dir: /app
